import React from "react";

import { StyledDropdown } from "~/components/partials/Header/FilterPanel/styles";

const CategoryFilter = (): JSX.Element => {
    return <StyledDropdown>Items</StyledDropdown>;
}

export default CategoryFilter;
