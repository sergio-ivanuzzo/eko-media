import React from "react";

import { MentionBarContainer } from "./styles";

const MentionBar = ({ negative, neutral, positive }: IMentionBarProps): JSX.Element => {
    return (
        <MentionBarContainer></MentionBarContainer>
    );
};

export default MentionBar;
