import React from "react";

import Header from "~/components/core/Header";

const App = (): JSX.Element => {
    return (
        <div>
            <Header />
        </div>
    );
};

export default App;
